apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: board-service-ingress
  namespace: flask-app
spec:
  ingressClassName: nginx
  rules:
    - host: board.labs.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: board-service
                port:
                  number: 5001
