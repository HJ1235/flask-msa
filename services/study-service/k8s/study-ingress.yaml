apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: study-service-ingress
  namespace: flask-app
spec:
  ingressClassName: nginx
  rules:
    - host: study.labs.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: study-service
                port:
                  number: 5004
