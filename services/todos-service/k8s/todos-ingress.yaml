apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todos-service-ingress
  namespace: flask-app
spec:
  ingressClassName: nginx
  rules:
    - host: todos.labs.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todos-service
                port:
                  number: 5003
